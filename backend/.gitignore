# Maven build output
target/

# Maven wrapper: the jar is not committed (distributionType=only-script),
# but keep the properties file and the mvnw scripts under version control.
.mvn/wrapper/maven-wrapper.jar

# IDE
.idea/
*.iml
*.iws
.vscode/
.classpath
.project
.settings/

# Logs
*.log

# Local environment overrides — never commit real credentials.
# Production secrets come from environment variables; see application-prod.yml.
application-local.yml
.env